{
  "name": "loanpro",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "prebuild": "prisma migrate up --experimental -c && prisma migrate up --experimental -c && prisma generate && node ./prisma/seed"
  },
  "dependencies": {
    "@prisma/client": "^2.7.1",
    "decimal.js": "^10.2.0",
    "next": "9.5.3",
    "next-connect": "^0.9.0",
    "nookies": "^2.4.0",
    "pg": "^8.3.3",
    "pg-hstore": "^2.3.3",
    "react": "16.13.1",
    "react-dom": "16.13.1"
  },
  "devDependencies": {
    "@prisma/cli": "^2.7.1"
  }
}
